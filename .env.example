# Copy to .env (or paste these as stack environment variables in Portainer).

# Connection string for your existing PostgreSQL. When the database runs as
# another container on the same Docker network, the host is its container name.
DATABASE_URL=postgres://wwm:CHANGE_ME@postgres:5432/wwm_guild

# Set to true only if your PostgreSQL requires TLS.
DATABASE_SSL=false

# Identifies this guild's rows. Change it if you ever host a second guild
# against the same database.
GUILD_ID=default-guild
GUILD_NAME=My Guild

# Host port the web UI is published on.
WEB_PORT=8085
